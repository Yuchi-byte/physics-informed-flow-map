# Per-checkout environment overrides, loaded by `start_run` before wandb init.
# Copy to `.secrets` (git-ignored) and fill in values. Empty values are skipped,
# and real environment variables take precedence over this file.

# Wandb entity (user or team) that runs upload to; empty = your wandb default entity.
WANDB_ENTITY=
