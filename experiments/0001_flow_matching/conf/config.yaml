defaults:
  - _self_
  - model: mlp
  - dataset: gaussians
  - training: default
  - sampling: default
  - experiment: gaussians

seed: 0

hydra:
  run:
    dir: /workspace/runs/0001_flow_matching/${now:%Y-%m-%dT%H-%M-%SZ}
  job:
    chdir: false
