# @package _global_
defaults:
  - override /model: dit
  - override /dataset: mnist

training:
  n_epochs: 100
  batch_size: 128
  eval_every_epochs: 10
  ckpt_every_epochs: 10
  ema:
    enabled: true
sampling:
  sampler_steps: 200
