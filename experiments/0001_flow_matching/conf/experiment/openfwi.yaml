# @package _global_
defaults:
  - override /model: dit
  - override /dataset: openfwi

training:
  n_epochs: 100
  batch_size: 64
  eval_every_epochs: 20
  ckpt_every_epochs: 20
  ema:
    enabled: true
sampling:
  sampler_steps: 200
  n_eval_samples: 64
