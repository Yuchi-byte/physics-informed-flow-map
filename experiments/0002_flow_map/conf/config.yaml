defaults:
  - _self_
  - model: mlp
  - dataset: gaussians
  - training: default
  - sampling: default
  - experiment: gaussians

seed: 0

hydra:
  run:
    dir: /workspace/runs/0002_flow_map/${dataset.name}_${training.distillation_type}_${now:%Y-%m-%dT%H-%M-%SZ}
  job:
    chdir: false
