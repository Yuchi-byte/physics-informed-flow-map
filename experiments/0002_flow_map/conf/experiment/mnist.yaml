# @package _global_
defaults:
  - override /model: dit
  - override /dataset: mnist

training:
  n_epochs: 50
  batch_size: 128
  eval_every_epochs: 5
  ckpt_every_epochs: 10  # local save + wandb artifact upload (also best + final)
  ema:
    enabled: true
sampling:
  sampler_steps: 200
