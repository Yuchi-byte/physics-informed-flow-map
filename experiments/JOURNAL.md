# Journal

One line per concluded finding. Newest last. Each line cites the framework
and run, and the headline metric.

Format: `NNNN_slug/variant — headline metric (evidence)`

<!-- e.g. 0001_mnist_pipeline/default — final loss 0.21 (200 steps, runs/0001_mnist_pipeline/2026-06-24T...Z) -->

- 0001_flow_matching/gaussians — pass: energy distance 0.009576 < 0.5 gate; 8 modes recovered (runs/0001_flow_matching/2026-06-24T204300Z)
- 0001_flow_matching/mnist — pass: final FM loss 158.02 < 240 gate; samples digit-like (runs/0001_flow_matching/2026-06-24T205632Z)
- 0001_flow_matching/gaussians — pass: energy distance 0.0104 < 0.5 gate (~2× the 0.0050 noise floor); epoch loop, 100 epochs / 200 sampler steps (runs/0001_flow_matching/2026-06-25T00-45-58Z)
- 0001_flow_matching/mnist — pass: final FM loss ~107 < 240 gate; clean digits across all 10 classes; DiT 256/6/8, 100 epochs, 200 sampler steps (runs/0001_flow_matching/2026-06-25T00-53-06Z)
- 0001_flow_matching/openfwi — energy distance 0.127 vs held-out reals (~1.4× the 0.090 floor); clean FlatVel_A flat-layer maps; DiT 256/6/8, 100 epochs, 200 sampler steps, EMA; best val_loss @ epoch 39; 500 steps worse (0.197) (runs/0001_flow_matching/2026-06-25T14-18-57Z)
- 0002_fwi_tilting/poc — DPS-for-flow inference-time tilting recovers a held-out FlatVel_A map: data misfit driven to 0.5% of unguided, best-of-4 posterior MAE ~300 m/s; recovered map on-manifold (top/bottom layers right, middle layer is the residual error). Finding: gradient normalization essential (raw guidance ~10% effect at best) (experiments/0002_fwi_tilting/poc.py; ζ=1.0, 200 steps) [CORRECTED 2026-06-25 — see below]
- 0002_fwi_tilting/poc — CORRECTION (2026-06-25): the above recovery claim does NOT yet stand. The target map was actually in the TRAINING set (global index 29999 lands in train under the seed-0 split), not held out; the headline "best-of-4 MAE" was an oracle best-of-N selected with ground truth (unavailable in a real inversion); and d_obs was generated by the same noiseless forward operator used in the guidance term (an inverse crime). poc.py is now fixed to (a) draw the target from the genuine validation split, (b) seed the run, (c) require an explicit --ckpt, (d) clamp velocities to the trained range, and (e) also report mean/median/worst MAE plus a ground-truth-free (min-data-misfit) pick. The recovery claim must be RE-RUN on a held-out map before it holds; no replacement numbers are asserted here.
- 0003_baselines/train_diffusion — diffusion-prior + canonical DPS baseline (camp-A comparison) added: diffusers UNet2DModel prior + DDPM training (physics_informed_flow_map.baselines.{build_denoiser,train_diffusion_prior}) and DPS sampler that backprops the wave-equation likelihood through the denoiser (baselines.dps_sample), inverting the same held-out map through the same Deepwave operator as the flow PoC. Plumbing verified (--smoke: misfit ratio 0.044). NOTE: same held-out-map caveats as the poc correction apply (re-run on a genuinely held-out, seeded, ground-truth-free basis before claiming recovery). Full-quality run + quantitative head-to-head (MAE/SSIM/#solves) deferred to the comparison spec. [REFACTORED 2026-06-26: train_diffusion.py → inversion.py — a Hydra entry that LOADS a run.py prior checkpoint (no inline retraining), draws the target from the genuine seed-0 validation split (leakage fixed, matching the poc correction), and selects the baseline via a `method` config group (dps | unguided; --multirun sweepable). run.py trains the prior; inversion.py inverts.]
- inversion comparison (flow tilting vs diffusion DPS) — first multi-map head-to-head via the shared `physics_informed_flow_map.inversion` Evaluator over 12 held-out FlatVel_A val maps, matched cost (800 PDE solves/inversion), guidance sweep {0.3,1.0,3.0}. Flow tilting (0002, DiT flow prior step_39_ema) beats diffusion DPS (0003, UNet prior step_99_ema) at EVERY config: GT-free MAE ~300-360 vs ≥426 m/s, oracle MAE ~207-227 vs ~357-667, SSIM ~0.78 vs ~0.72-0.77. Flow is robust across guidance; diffusion DPS is fragile (best @ g=0.3, degrades to 753 @ g=3). KEY: the GT-free (min-misfit) pick trails the oracle by ~100-150 m/s for both — posterior selection, not prior quality, is the bottleneck; posterior-mean is no better. CAVEAT: NOT a clean mechanism comparison — priors differ (well-tuned flow vs fresh diffusion), so the gap conflates prior quality with tilting-vs-DPS; single seed, n=4, inverse crime. Reproduce: notebooks/inversion_eval.ipynb (scratchpad multimap_sweep.py).
- inversion comparison (all camp-A inverters, normalized-scale) — 9 methods over 4 held-out FlatVel_A maps, matched 800 PDE-solves/inversion, metrics = expected MAE/RMSE/SSIM across posterior samples on the [-1,1] OpenFWI scale (notebooks/run_inversion_eval.py; runs/inversion_eval/results.md). Ranking by MAE: fm·tilt·g1 0.176 / SSIM 0.503 (best model error + structure); red_diffeq·dit 0.180; classical·tv 0.181 (lowest data misfit 1.8e3 — fits data best with NO learned prior); flowmap·tilt·g1 0.189; unguided controls fm·g0 0.292 / flowmap·g0 0.305; classical·tikhonov 0.315 (TV >> Tikhonov for blocky layered media); diff_dit·dps·g0.3 0.325; diff_unet·dps·g0.3 0.341. FINDINGS: (1) wave-equation guidance cuts MAE ~40% vs unguided prior; (2) flow-matching tilt is the best method on model error + SSIM; (3) the optimization baselines (RED-DiffEq, classical-TV) tie fm·tilt on MAE and crush it on DATA misfit — the inverse-crime/non-uniqueness gap (fitting noiseless data ≠ low model error); (4) DPS underperforms even the unguided flow prior, but its g=0.3 is UNtuned. CAVEATS: inverse crime (noiseless, same operator) favors the data-fitting optimizers; n=4, single seed; DPS guidance not swept.
- 0002_flow_map/teacher-distill — esd_teacher distillation of the flow-map prior from the 0001 flow-matching prior (warm-start + off-diagonal esd_teacher target; 100 epochs FlatVel_A; runs/0002_flow_map/2026-06-27T01-44-09Z). vs the from-scratch (mf) flow-map on 4 held-out maps @ 800 solves (tilt g1): SSIM 0.444 -> 0.496 (to flow-matching parity 0.502), MAE 0.189 -> 0.192 (a wash, within the ±0.05 across-map std), RMSE 0.246 -> 0.261. FINDING: distillation buys flow-matching-like STRUCTURE (SSIM) but not its lower model error (MAE) — the flow-map still trails the fm champion (MAE 0.176) on model error. Honest, modest: SSIM is the clear signal; MAE differences across the three priors (0.176/0.189/0.192) are within ~1 std at n=4. CAVEAT: n=4, single seed, inverse crime.
- inversion eval n=8 + flow sample sweep (runs/inversion_eval/results_n8.md) — at 8 held-out FlatVel_A maps the n=4 rankings HOLD: fm·tilt best (MAE 0.219/SSIM 0.45), classical·tv 0.225 (misfit 3.4e3, best data fit), red_diffeq 0.226, flowmap_teacher·tilt 0.238; MAE rises ~0.18->0.22 vs n=4 (n=4 was slightly easy, ordering unchanged). KEY: the flow-tilt posterior-sample sweep n_samples {4,8,16} does NOT improve the metric (fm·tilt 0.219/0.241/0.223 — flat within noise). Expected: the expected-PER-SAMPLE metric is invariant to sample count; more samples only tightens the estimate or helps a best-of-N / posterior-mean SELECTION (both removed earlier). So "more samples to the inverse process" pays off only if a GT-free selection rule is reintroduced.
- larger-net comparison (320/8 ~16M vs 256/6 ~8M, n=8 FlatVel_A; runs/inversion_eval/results_larger_net.md) — FINDING: capacity helps the FLOW-MAP, not flow-matching. fm 256/6 -> 320/8 MAE 0.219 -> 0.221 (flat, within noise; FlatVel_A saturates ~8M params for the noise->data objective). flow-map 320/8 MAE 0.213 = best here, beating the smaller teacher-distilled flow-map (0.238 @ n=8) — the harder s->u map objective is capacity-hungry. SSIM: fm_small 0.449 still highest, flowmap_large 0.431. CAVEAT: all gaps within the ±0.04 across-map std at n=8 — a pattern, not a decisive win. Larger fm runs/0001_flow_matching/2026-06-27T05-08-13Z (val 84.2), larger flowmap runs/0002_flow_map/2026-06-27T05-57-07Z (val 94.6); both 120 epochs.
- inversion on harder family CurveVel_A (runs/inversion_eval/results_CurveVel_A.md; fm prior runs/0001_flow_matching/2026-06-27T08-10-42Z, 100ep, val 172) — 8 held-out maps: classical·tv MAE 0.256/SSIM 0.323/misfit 2.8e3, fm·tilt 0.262/0.404/2.8e4, fm·prior(unguided) 0.392/0.265. FINDINGS: (1) on the curved family classical·tv TIES/edges the learned prior on MAE but the prior wins clearly on SSIM (0.404 vs 0.323) — the learned prior's robust edge is STRUCTURE, not pixel error; (2) all harder than FlatVel (MAE ~0.26 vs ~0.22); (3) guidance still cuts MAE ~33% (0.392->0.262). CAVEAT: inverse crime (noiseless, same op) strongly flatters classical·tv, which directly minimizes the misfit — measurement noise would hurt it more than the prior. n=8, single seed.
- combined multi-family prior (FlatVel_A+CurveVel_A+FlatFault_A+FlatVel_B, 60ep, 256/6; runs/0001_flow_matching/2026-06-27T08-38-54Z, val 148.5; runs/inversion_eval/results_combined.md) — fm·tilt on each family's held-out maps: FlatVel_A 0.269 (vs specialized 0.219), CurveVel_A 0.306 (vs 0.262), FlatFault_A 0.330 (hardest, faults). The combined prior UNDERPERFORMS specialized single-family priors by ~0.04-0.05 MAE per family. CAVEAT: confounded — combined trained only 60 epochs (vs 100) and splits capacity across 4 families; shows the cost of generalization, not a clean controlled test. A fair test needs the combined prior trained longer/larger.
- inversion n=64 core comparison (runs/inversion_eval/results_n64.md) — tightened error bars (SEM ≈ std/8 ≈ 0.012): fm·tilt 0.2425±0.093 (best) < red_diffeq 0.2498 < classical·tv 0.2543 < flowmap_teacher·tilt 0.2621. Rankings from n=8 HOLD but MAEs are higher/more honest at n=64 (n=8 was optimistic). fm·tilt also best SSIM (0.460). flowmap (DPS tilt) trails fm here.
- MFM-G native steering, first tuning (FlowMapSteerModule, sne/iwae, runs/inversion_eval/results_steer_tune.md) — the non-Tweedie steering DRIVES DATA MISFIT DOWN HARD (base 4.2e5 -> sne 5e4, ~10x) but MAE EXPLODES (0.284 -> 0.52-0.62): classic off-manifold data-fitting (non-uniqueness under the inverse crime). Diagnosis: guidance_scale=1.0 with renorm_gradient=True forces steering magnitude == base flow drift == far too strong; sigma is not the lever, guidance_scale is. Next: guidance_scale sweep down {0.03,0.1,0.3}. The mechanism works (misfit responds correctly); it needs the prior to stay in control.
- MFM-G steering RESOLVED via renorm=False (runs/inversion_eval/results_steer_norenorm.md) — renorm_gradient=True was pinning the steering to ||base_drift|| every step, preventing the reward gradient from self-attenuating; ALL estimators (dps/sne/iwae) over-fit identically under it (dps·gs0.1 MAE 0.406, misfit crash to 1.7e4 — same as sne/iwae, so it was the FRAMEWORK not MC-vs-Tweedie). With renorm=False and sigma as the (self-attenuating) strength knob: well-behaved. iwae·sig1000 MAE 0.280 (vs base 0.284, flat) with SSIM 0.397 vs base 0.342 (+0.055 consistent across all 6 steered configs) and misfit moderately reduced (4.2e5->3.2e5, no crash). sigma~1000 sweet spot; sig300 starts over-fitting. So the non-Tweedie steering gives a gentle on-manifold nudge that improves STRUCTURE (SSIM) at flat MAE; the MAE ceiling is the inverse crime (data-fit != model accuracy), motivating the measurement-noise experiment. CAVEAT: n=6, SSIM gain ~1 SEM — validate at higher n.
- MFM-G steering validation at n=32 (runs/inversion_eval/results_steer_n32.md) — the n=6 SSIM gain did NOT hold. Gentle renorm=False steering (sig1000) vs base: MAE 0.385 vs 0.389 (flat), SSIM 0.298 vs 0.275 (+0.023 ~ 1 SEM, NOT significant), misfit ~unchanged (4.1e5 vs 4.28e5). CONCLUSION: under the noiseless inverse crime there is NO steering strength that significantly helps — strong steering over-fits (MAE explodes), gentle does nothing. The method is correct/well-behaved (renorm=False) but the benchmark can't reward data-fitting (prior already on-manifold; data adds no info beyond exploitable degeneracy). This critique applies to ALL guided methods compared so far (dps/red/classical/mfm) — they were judged where guidance can't win. THE unblocking experiment is measurement noise on d_obs with sigma matched: only then does data-fitting become informative and posterior-sampling-vs-overfitting matter. Needs Evaluator noise_std param.
- RENAMED 2026-07-02: 0003_baselines → 0003_diffusion — the name was stale: since the 2026-06-26 refactor moved inversion out, 0003 only trains the diffusion prior (parallel to 0001_flow_matching / 0002_flow_map), and the actual benchmarked baseline methods (dps/classical_fwi/red_diffeq/unguided/…) live in 0004_inversion's `method` group. All code/config/notebook refs updated; runs/0003_baselines is now a symlink to runs/0003_diffusion (W&B history keeps the old name).
- 0004_inversion/mfm_g mc_samples 4→20 (single held-out map 6044, esd_teacher flow-map ckpt, σ1000/renorm=F/SDE, 200 steps) — metrics IDENTICAL to mc=4 (MAE 0.2160 vs 0.2157, SSIM 0.356 vs 0.356, misfit_ratio 0.735 vs 0.724) at 5× the solves (16k vs 3.2k). mc=64 is infeasible on 32 GB (mc=20 already peaks 24.7 GiB — the IWAE grad holds all n_samples×mc wave-solve graphs at once). CONFIRMS the n=32 conclusion from the MC side: at σ1000 the steering signal is so gentle that MC-estimate quality is irrelevant — draw count is NOT the lever. mfm_g still far behind the simple Tweedie flow_tilt on the same target+ckpt (flow_tilt MAE 0.127/SSIM 0.635/misfit_ratio 0.072 @ 800 solves vs mfm_g 0.216/0.356/0.73 @ 16k). (runs/0004_inversion/flow_map_mfm_g_2026-07-02T22-03-01Z)
- 0004_inversion/mfm_g sigma/renorm sweep on the same single map 6044 (mc=4, 200 steps) — σ100/renorm=F is the best mfm_g config: MAE 0.195/SSIM 0.431/misfit_ratio 0.077 (vs σ1000's 0.216/0.356/0.73; σ300 in between at 0.193/0.373/0.14; renorm=T at σ1000 explodes MAE to 0.459 exactly as the earlier eval-scale tuning found). KEY MECHANISM FINDING: at σ100 mfm_g fits the data as hard as flow_tilt (misfit_ratio 0.077 vs 0.072) yet still loses badly on model error (MAE 0.195 vs 0.127, SSIM 0.431 vs 0.635) — the gap is NOT steering strength but the DIRECTION of the likelihood gradient: the Tweedie chain rule (through the network Jacobian at x_t) stays manifold-aligned, while the IWAE gradient through fresh-noise one-step posterior draws finds off-manifold ways to cut the misfit. Suspect the t_cond>0 conditional channel's x_cond-sensitivity (trained with t_cond_power=2, uncond_prob=0.1, never directly validated). PAPER CHECK (arXiv 2601.14430 F.3.2/F.3.6): the paper's ImageNet practice is ODE (250 Euler steps) + steering-drift RESCALE to ||base_drift|| with λ=1 (Eq. 136) — i.e. renorm=true IS the paper's practical setting (the mfm_g.yaml comment claimed otherwise); their GMM inverse problem uses the TRUE observation noise as sigma, reinforcing that our noiseless-inverse-crime sigma is unprincipled; MFM-GF "consistently inferior" (don't try it); untried alternative: MFM-Search (Algorithm 4, per-step best-of-N resampling — repo has euler_sampler_tree_search), attractive given the earlier finding that posterior SELECTION is the bottleneck. (runs/0004_inversion/flow_map_mfm_g_2026-07-02T{22-47-38,22-52-34,22-57-39}Z)
- PICKUP 2026-07-02 (session sign-off) — where the mfm_g investigation stands and the ranked hypotheses. STATE: mfm_g best config now σ100/renorm=F/mc=4 (yaml default updated); at that setting it matches flow_tilt's data fit (misfit_ratio 0.077 vs 0.072) but loses on model error (MAE 0.195 vs 0.127, SSIM 0.431 vs 0.635) on single map 6044, esd_teacher ckpt. Draw count (mc 4→20) and estimator family (gf per paper) are ELIMINATED as levers; renorm=T eliminated (over-fits). EDUCATED GUESSES, ranked: (1) the t_cond>0 conditional channel is the weak link — MFM-G's edge over Tweedie rests entirely on the one-step posterior p(x1|xt) being accurate AND x_cond-sensitive; ours was trained (t_cond_power=2, uncond_prob=0.1) but never validated. TEST: from a fixed x_t at several t, compare one-step posterior draws vs many-ODE-step conditional rollouts from the same x_t (distributional check, no inversion needed — cheap, do this first). (2) The IWAE gradient is manifold-unaligned for hard data-consistency rewards: Tweedie's chain rule through the network Jacobian acts as a manifold projector; the posterior-draw gradient has no such projection, so equal misfit ≠ equal model error under noiseless non-uniqueness. If (1) checks out clean, this becomes the primary explanation and the fix is problem-side, not model-side → (3). (3) The benchmark can't reward posterior quality: noiseless inverse crime means sigma is unprincipled and data-fitting degeneracy is exploitable (the n=32 conclusion). THE unblocking experiment remains: Evaluator noise_std param, noise on d_obs with likelihood sigma MATCHED to it — the setting where MFM-G's less-biased posterior should finally show up vs flow_tilt/classical, per the paper's GMM protocol (true σ in the reward). NEXT ACTIONS, in order: (a) posterior-fidelity check (1); (b) add noise_std + matched-σ eval; (c) wire MFM-Search (euler_sampler_tree_search, already in mfm repo) as a 0004 method — untried, gradient-free, and targets the known posterior-SELECTION bottleneck; (d) only then revisit per-family eval (flatfault.yaml is staged for this, never run, needs 320/8 ckpts).
- 0004_inversion/fmrg_e first run — guidance sweep {0.3,1.0,3.0} + normalize_grad={T,F} on single map 6044, esd_teacher ckpt, 200 steps, n_samples=4 (800 solves). BEST: normalize=T, g=0.3: MAE 0.137/SSIM 0.578/misfit_ratio 0.013. REFERENCE: flow_tilt g=1 norm=T: MAE 0.127/SSIM 0.635/misfit_ratio 0.072. FMRG-E does NOT beat flow_tilt at matched solve budget (n_opt=1). SHARP CLIFF: g=0.3 → good (MAE 0.137), g=1.0 → broken (MAE 0.676), g=3.0 → worst (MAE 0.899). normalize_grad=False at g=1.0 diverges completely (MAE 0.884, misfit_ratio 2.07 — guidance INCREASES the data error, raw seismic gradient is enormous). KEY MECHANISM: FMRG-E at g=0.3 drives misfit_ratio to 0.013 (5× lower than flow_tilt's 0.072) yet has WORSE model error (MAE 0.137 vs 0.127) — fits data harder but goes more off-manifold. Two reasons: (1) wt=(1-t)*t_next decays to zero at t≈0, suppressing guidance during the early trajectory steps where rough velocity structure forms (flow_tilt applies constant guidance from step 1); (2) inner gradient is in x1-space and NOT manifold-aligned — FMRG-E's n_opt=1 single step in x1-space differs from flow_tilt's chain-rule gradient in xt-space which implicitly projects onto the trajectory tangent. BOTTOM LINE: FMRG-E (n_opt=1) is a DPS variant with FMRG time-weighting, but the wt schedule that's optimal by control theory is suboptimal for the FWI recovery objective under the inverse crime. (runs/0004_inversion/flow_map_fmrg_e_2026-07-03T00-08-01Z/{0,1,2}; 5n5nfb0z)
