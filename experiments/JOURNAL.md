# Journal

One line per concluded finding. Newest last. Each line cites the framework
and run, and the headline metric.

Format: `NNNN_slug/variant — headline metric (evidence)`

<!-- e.g. 0001_mnist_pipeline/default — final loss 0.21 (200 steps, runs/0001_mnist_pipeline/2026-06-24T...Z) -->

- 0001_flow_matching/gaussians — pass: energy distance 0.009576 < 0.5 gate; 8 modes recovered (runs/0001_flow_matching/2026-06-24T204300Z)
- 0001_flow_matching/mnist — pass: final FM loss 158.02 < 240 gate; samples digit-like (runs/0001_flow_matching/2026-06-24T205632Z)
- 0001_flow_matching/gaussians — pass: energy distance 0.0104 < 0.5 gate (~2× the 0.0050 noise floor); epoch loop, 100 epochs / 200 sampler steps (runs/0001_flow_matching/2026-06-25T00-45-58Z)
- 0001_flow_matching/mnist — pass: final FM loss ~107 < 240 gate; clean digits across all 10 classes; DiT 256/6/8, 100 epochs, 200 sampler steps (runs/0001_flow_matching/2026-06-25T00-53-06Z)
- 0001_flow_matching/openfwi — energy distance 0.127 vs held-out reals (~1.4× the 0.090 floor); clean FlatVel_A flat-layer maps; DiT 256/6/8, 100 epochs, 200 sampler steps, EMA; best val_loss @ epoch 39; 500 steps worse (0.197) (runs/0001_flow_matching/2026-06-25T14-18-57Z)
- 0002_fwi_tilting/poc — DPS-for-flow inference-time tilting recovers a held-out FlatVel_A map: data misfit driven to 0.5% of unguided, best-of-4 posterior MAE ~300 m/s; recovered map on-manifold (top/bottom layers right, middle layer is the residual error). Finding: gradient normalization essential (raw guidance ~10% effect at best) (experiments/0002_fwi_tilting/poc.py; ζ=1.0, 200 steps) [CORRECTED 2026-06-25 — see below]
- 0002_fwi_tilting/poc — CORRECTION (2026-06-25): the above recovery claim does NOT yet stand. The target map was actually in the TRAINING set (global index 29999 lands in train under the seed-0 split), not held out; the headline "best-of-4 MAE" was an oracle best-of-N selected with ground truth (unavailable in a real inversion); and d_obs was generated by the same noiseless forward operator used in the guidance term (an inverse crime). poc.py is now fixed to (a) draw the target from the genuine validation split, (b) seed the run, (c) require an explicit --ckpt, (d) clamp velocities to the trained range, and (e) also report mean/median/worst MAE plus a ground-truth-free (min-data-misfit) pick. The recovery claim must be RE-RUN on a held-out map before it holds; no replacement numbers are asserted here.
- 0003_baselines/train_diffusion — diffusion-prior + canonical DPS baseline (camp-A comparison) added: diffusers UNet2DModel prior + DDPM training (physics_informed_flow_map.baselines.{build_denoiser,train_diffusion_prior}) and DPS sampler that backprops the wave-equation likelihood through the denoiser (baselines.dps_sample), inverting the same held-out map through the same Deepwave operator as the flow PoC. Plumbing verified (--smoke: misfit ratio 0.044). NOTE: same held-out-map caveats as the poc correction apply (re-run on a genuinely held-out, seeded, ground-truth-free basis before claiming recovery). Full-quality run + quantitative head-to-head (MAE/SSIM/#solves) deferred to the comparison spec. [REFACTORED 2026-06-26: train_diffusion.py → inversion.py — a Hydra entry that LOADS a run.py prior checkpoint (no inline retraining), draws the target from the genuine seed-0 validation split (leakage fixed, matching the poc correction), and selects the baseline via a `method` config group (dps | unguided; --multirun sweepable). run.py trains the prior; inversion.py inverts.]
- inversion comparison (flow tilting vs diffusion DPS) — first multi-map head-to-head via the shared `physics_informed_flow_map.inversion` Evaluator over 12 held-out FlatVel_A val maps, matched cost (800 PDE solves/inversion), guidance sweep {0.3,1.0,3.0}. Flow tilting (0002, DiT flow prior step_39_ema) beats diffusion DPS (0003, UNet prior step_99_ema) at EVERY config: GT-free MAE ~300-360 vs ≥426 m/s, oracle MAE ~207-227 vs ~357-667, SSIM ~0.78 vs ~0.72-0.77. Flow is robust across guidance; diffusion DPS is fragile (best @ g=0.3, degrades to 753 @ g=3). KEY: the GT-free (min-misfit) pick trails the oracle by ~100-150 m/s for both — posterior selection, not prior quality, is the bottleneck; posterior-mean is no better. CAVEAT: NOT a clean mechanism comparison — priors differ (well-tuned flow vs fresh diffusion), so the gap conflates prior quality with tilting-vs-DPS; single seed, n=4, inverse crime. Reproduce: notebooks/inversion_eval.ipynb (scratchpad multimap_sweep.py).
- inversion comparison (all camp-A inverters, normalized-scale) — 9 methods over 4 held-out FlatVel_A maps, matched 800 PDE-solves/inversion, metrics = expected MAE/RMSE/SSIM across posterior samples on the [-1,1] OpenFWI scale (notebooks/run_inversion_eval.py; runs/inversion_eval/results.md). Ranking by MAE: fm·tilt·g1 0.176 / SSIM 0.503 (best model error + structure); red_diffeq·dit 0.180; classical·tv 0.181 (lowest data misfit 1.8e3 — fits data best with NO learned prior); flowmap·tilt·g1 0.189; unguided controls fm·g0 0.292 / flowmap·g0 0.305; classical·tikhonov 0.315 (TV >> Tikhonov for blocky layered media); diff_dit·dps·g0.3 0.325; diff_unet·dps·g0.3 0.341. FINDINGS: (1) wave-equation guidance cuts MAE ~40% vs unguided prior; (2) flow-matching tilt is the best method on model error + SSIM; (3) the optimization baselines (RED-DiffEq, classical-TV) tie fm·tilt on MAE and crush it on DATA misfit — the inverse-crime/non-uniqueness gap (fitting noiseless data ≠ low model error); (4) DPS underperforms even the unguided flow prior, but its g=0.3 is UNtuned. CAVEATS: inverse crime (noiseless, same operator) favors the data-fitting optimizers; n=4, single seed; DPS guidance not swept.
- 0002_flow_map/teacher-distill — esd_teacher distillation of the flow-map prior from the 0001 flow-matching prior (warm-start + off-diagonal esd_teacher target; 100 epochs FlatVel_A; runs/0002_flow_map/2026-06-27T01-44-09Z). vs the from-scratch (mf) flow-map on 4 held-out maps @ 800 solves (tilt g1): SSIM 0.444 -> 0.496 (to flow-matching parity 0.502), MAE 0.189 -> 0.192 (a wash, within the ±0.05 across-map std), RMSE 0.246 -> 0.261. FINDING: distillation buys flow-matching-like STRUCTURE (SSIM) but not its lower model error (MAE) — the flow-map still trails the fm champion (MAE 0.176) on model error. Honest, modest: SSIM is the clear signal; MAE differences across the three priors (0.176/0.189/0.192) are within ~1 std at n=4. CAVEAT: n=4, single seed, inverse crime.
- inversion eval n=8 + flow sample sweep (runs/inversion_eval/results_n8.md) — at 8 held-out FlatVel_A maps the n=4 rankings HOLD: fm·tilt best (MAE 0.219/SSIM 0.45), classical·tv 0.225 (misfit 3.4e3, best data fit), red_diffeq 0.226, flowmap_teacher·tilt 0.238; MAE rises ~0.18->0.22 vs n=4 (n=4 was slightly easy, ordering unchanged). KEY: the flow-tilt posterior-sample sweep n_samples {4,8,16} does NOT improve the metric (fm·tilt 0.219/0.241/0.223 — flat within noise). Expected: the expected-PER-SAMPLE metric is invariant to sample count; more samples only tightens the estimate or helps a best-of-N / posterior-mean SELECTION (both removed earlier). So "more samples to the inverse process" pays off only if a GT-free selection rule is reintroduced.
- larger-net comparison (320/8 ~16M vs 256/6 ~8M, n=8 FlatVel_A; runs/inversion_eval/results_larger_net.md) — FINDING: capacity helps the FLOW-MAP, not flow-matching. fm 256/6 -> 320/8 MAE 0.219 -> 0.221 (flat, within noise; FlatVel_A saturates ~8M params for the noise->data objective). flow-map 320/8 MAE 0.213 = best here, beating the smaller teacher-distilled flow-map (0.238 @ n=8) — the harder s->u map objective is capacity-hungry. SSIM: fm_small 0.449 still highest, flowmap_large 0.431. CAVEAT: all gaps within the ±0.04 across-map std at n=8 — a pattern, not a decisive win. Larger fm runs/0001_flow_matching/2026-06-27T05-08-13Z (val 84.2), larger flowmap runs/0002_flow_map/2026-06-27T05-57-07Z (val 94.6); both 120 epochs.
- inversion on harder family CurveVel_A (runs/inversion_eval/results_CurveVel_A.md; fm prior runs/0001_flow_matching/2026-06-27T08-10-42Z, 100ep, val 172) — 8 held-out maps: classical·tv MAE 0.256/SSIM 0.323/misfit 2.8e3, fm·tilt 0.262/0.404/2.8e4, fm·prior(unguided) 0.392/0.265. FINDINGS: (1) on the curved family classical·tv TIES/edges the learned prior on MAE but the prior wins clearly on SSIM (0.404 vs 0.323) — the learned prior's robust edge is STRUCTURE, not pixel error; (2) all harder than FlatVel (MAE ~0.26 vs ~0.22); (3) guidance still cuts MAE ~33% (0.392->0.262). CAVEAT: inverse crime (noiseless, same op) strongly flatters classical·tv, which directly minimizes the misfit — measurement noise would hurt it more than the prior. n=8, single seed.
- combined multi-family prior (FlatVel_A+CurveVel_A+FlatFault_A+FlatVel_B, 60ep, 256/6; runs/0001_flow_matching/2026-06-27T08-38-54Z, val 148.5; runs/inversion_eval/results_combined.md) — fm·tilt on each family's held-out maps: FlatVel_A 0.269 (vs specialized 0.219), CurveVel_A 0.306 (vs 0.262), FlatFault_A 0.330 (hardest, faults). The combined prior UNDERPERFORMS specialized single-family priors by ~0.04-0.05 MAE per family. CAVEAT: confounded — combined trained only 60 epochs (vs 100) and splits capacity across 4 families; shows the cost of generalization, not a clean controlled test. A fair test needs the combined prior trained longer/larger.
- inversion n=64 core comparison (runs/inversion_eval/results_n64.md) — tightened error bars (SEM ≈ std/8 ≈ 0.012): fm·tilt 0.2425±0.093 (best) < red_diffeq 0.2498 < classical·tv 0.2543 < flowmap_teacher·tilt 0.2621. Rankings from n=8 HOLD but MAEs are higher/more honest at n=64 (n=8 was optimistic). fm·tilt also best SSIM (0.460). flowmap (DPS tilt) trails fm here.
- MFM-G native steering, first tuning (FlowMapSteerModule, sne/iwae, runs/inversion_eval/results_steer_tune.md) — the non-Tweedie steering DRIVES DATA MISFIT DOWN HARD (base 4.2e5 -> sne 5e4, ~10x) but MAE EXPLODES (0.284 -> 0.52-0.62): classic off-manifold data-fitting (non-uniqueness under the inverse crime). Diagnosis: guidance_scale=1.0 with renorm_gradient=True forces steering magnitude == base flow drift == far too strong; sigma is not the lever, guidance_scale is. Next: guidance_scale sweep down {0.03,0.1,0.3}. The mechanism works (misfit responds correctly); it needs the prior to stay in control.
- MFM-G steering RESOLVED via renorm=False (runs/inversion_eval/results_steer_norenorm.md) — renorm_gradient=True was pinning the steering to ||base_drift|| every step, preventing the reward gradient from self-attenuating; ALL estimators (dps/sne/iwae) over-fit identically under it (dps·gs0.1 MAE 0.406, misfit crash to 1.7e4 — same as sne/iwae, so it was the FRAMEWORK not MC-vs-Tweedie). With renorm=False and sigma as the (self-attenuating) strength knob: well-behaved. iwae·sig1000 MAE 0.280 (vs base 0.284, flat) with SSIM 0.397 vs base 0.342 (+0.055 consistent across all 6 steered configs) and misfit moderately reduced (4.2e5->3.2e5, no crash). sigma~1000 sweet spot; sig300 starts over-fitting. So the non-Tweedie steering gives a gentle on-manifold nudge that improves STRUCTURE (SSIM) at flat MAE; the MAE ceiling is the inverse crime (data-fit != model accuracy), motivating the measurement-noise experiment. CAVEAT: n=6, SSIM gain ~1 SEM — validate at higher n.
- MFM-G steering validation at n=32 (runs/inversion_eval/results_steer_n32.md) — the n=6 SSIM gain did NOT hold. Gentle renorm=False steering (sig1000) vs base: MAE 0.385 vs 0.389 (flat), SSIM 0.298 vs 0.275 (+0.023 ~ 1 SEM, NOT significant), misfit ~unchanged (4.1e5 vs 4.28e5). CONCLUSION: under the noiseless inverse crime there is NO steering strength that significantly helps — strong steering over-fits (MAE explodes), gentle does nothing. The method is correct/well-behaved (renorm=False) but the benchmark can't reward data-fitting (prior already on-manifold; data adds no info beyond exploitable degeneracy). This critique applies to ALL guided methods compared so far (dps/red/classical/mfm) — they were judged where guidance can't win. THE unblocking experiment is measurement noise on d_obs with sigma matched: only then does data-fitting become informative and posterior-sampling-vs-overfitting matter. Needs Evaluator noise_std param.
- RENAMED 2026-07-02: 0003_baselines → 0003_diffusion — the name was stale: since the 2026-06-26 refactor moved inversion out, 0003 only trains the diffusion prior (parallel to 0001_flow_matching / 0002_flow_map), and the actual benchmarked baseline methods (dps/classical_fwi/red_diffeq/unguided/…) live in 0004_inversion's `method` group. All code/config/notebook refs updated; runs/0003_baselines is now a symlink to runs/0003_diffusion (W&B history keeps the old name).
- 0004_inversion/mfm_g mc_samples 4→20 (single held-out map 6044, esd_teacher flow-map ckpt, σ1000/renorm=F/SDE, 200 steps) — metrics IDENTICAL to mc=4 (MAE 0.2160 vs 0.2157, SSIM 0.356 vs 0.356, misfit_ratio 0.735 vs 0.724) at 5× the solves (16k vs 3.2k). mc=64 is infeasible on 32 GB (mc=20 already peaks 24.7 GiB — the IWAE grad holds all n_samples×mc wave-solve graphs at once). CONFIRMS the n=32 conclusion from the MC side: at σ1000 the steering signal is so gentle that MC-estimate quality is irrelevant — draw count is NOT the lever. mfm_g still far behind the simple Tweedie flow_tilt on the same target+ckpt (flow_tilt MAE 0.127/SSIM 0.635/misfit_ratio 0.072 @ 800 solves vs mfm_g 0.216/0.356/0.73 @ 16k). (runs/0004_inversion/flow_map_mfm_g_2026-07-02T22-03-01Z)
- 0004_inversion/mfm_g sigma/renorm sweep on the same single map 6044 (mc=4, 200 steps) — σ100/renorm=F is the best mfm_g config: MAE 0.195/SSIM 0.431/misfit_ratio 0.077 (vs σ1000's 0.216/0.356/0.73; σ300 in between at 0.193/0.373/0.14; renorm=T at σ1000 explodes MAE to 0.459 exactly as the earlier eval-scale tuning found). KEY MECHANISM FINDING: at σ100 mfm_g fits the data as hard as flow_tilt (misfit_ratio 0.077 vs 0.072) yet still loses badly on model error (MAE 0.195 vs 0.127, SSIM 0.431 vs 0.635) — the gap is NOT steering strength but the DIRECTION of the likelihood gradient: the Tweedie chain rule (through the network Jacobian at x_t) stays manifold-aligned, while the IWAE gradient through fresh-noise one-step posterior draws finds off-manifold ways to cut the misfit. Suspect the t_cond>0 conditional channel's x_cond-sensitivity (trained with t_cond_power=2, uncond_prob=0.1, never directly validated). PAPER CHECK (arXiv 2601.14430 F.3.2/F.3.6): the paper's ImageNet practice is ODE (250 Euler steps) + steering-drift RESCALE to ||base_drift|| with λ=1 (Eq. 136) — i.e. renorm=true IS the paper's practical setting (the mfm_g.yaml comment claimed otherwise); their GMM inverse problem uses the TRUE observation noise as sigma, reinforcing that our noiseless-inverse-crime sigma is unprincipled; MFM-GF "consistently inferior" (don't try it); untried alternative: MFM-Search (Algorithm 4, per-step best-of-N resampling — repo has euler_sampler_tree_search), attractive given the earlier finding that posterior SELECTION is the bottleneck. (runs/0004_inversion/flow_map_mfm_g_2026-07-02T{22-47-38,22-52-34,22-57-39}Z)
- PICKUP 2026-07-02 (session sign-off) — where the mfm_g investigation stands and the ranked hypotheses. STATE: mfm_g best config now σ100/renorm=F/mc=4 (yaml default updated); at that setting it matches flow_tilt's data fit (misfit_ratio 0.077 vs 0.072) but loses on model error (MAE 0.195 vs 0.127, SSIM 0.431 vs 0.635) on single map 6044, esd_teacher ckpt. Draw count (mc 4→20) and estimator family (gf per paper) are ELIMINATED as levers; renorm=T eliminated (over-fits). EDUCATED GUESSES, ranked: (1) the t_cond>0 conditional channel is the weak link — MFM-G's edge over Tweedie rests entirely on the one-step posterior p(x1|xt) being accurate AND x_cond-sensitive; ours was trained (t_cond_power=2, uncond_prob=0.1) but never validated. TEST: from a fixed x_t at several t, compare one-step posterior draws vs many-ODE-step conditional rollouts from the same x_t (distributional check, no inversion needed — cheap, do this first). (2) The IWAE gradient is manifold-unaligned for hard data-consistency rewards: Tweedie's chain rule through the network Jacobian acts as a manifold projector; the posterior-draw gradient has no such projection, so equal misfit ≠ equal model error under noiseless non-uniqueness. If (1) checks out clean, this becomes the primary explanation and the fix is problem-side, not model-side → (3). (3) The benchmark can't reward posterior quality: noiseless inverse crime means sigma is unprincipled and data-fitting degeneracy is exploitable (the n=32 conclusion). THE unblocking experiment remains: Evaluator noise_std param, noise on d_obs with likelihood sigma MATCHED to it — the setting where MFM-G's less-biased posterior should finally show up vs flow_tilt/classical, per the paper's GMM protocol (true σ in the reward). NEXT ACTIONS, in order: (a) posterior-fidelity check (1); (b) add noise_std + matched-σ eval; (c) wire MFM-Search (euler_sampler_tree_search, already in mfm repo) as a 0004 method — untried, gradient-free, and targets the known posterior-SELECTION bottleneck; (d) only then revisit per-family eval (flatfault.yaml is staged for this, never run, needs 320/8 ckpts).
- PICKUP 2026-07-03 (next experiments for FMRG-E) — two concrete follow-ups from the first run. (A) n_opt > 1 at smaller g: try n_opt=5, g=0.05 (same 800 solves = 200 steps × 1 wave solve × 4 samples... wait, n_opt=5 means 5 wave solves per outer step → 4000 solves total — that's 5× the flow_tilt budget). To stay at 800 solves with n_opt=5 use steps=40 (40 steps × 5 inner × 4 samples = 800). Or accept the 5× cost and see if n_opt=5 at larger steps (200) beats flow_tilt with more solves — a "is extra compute worth it?" question. Run: `method.n_opt=5 method.guidance_strength=0.05 steps=40` (budget-matched) vs `method.n_opt=5 method.guidance_strength=0.05 steps=200` (5× budget). (B) FMRG-J: differentiates through the MFM model's own Jacobian (∂v/∂xt), keeping updates on the trajectory manifold by construction. Costs 1 model backward pass per outer step (no extra wave solves vs flow_tilt — the gradient flows from wave solver through Tweedie through model). Implement in inversion/fmrg.py as `fmrg_j_sample`, analogous to `reward_consistency_xt` in fluxfm_sampler_ip.py: compute v = prior.v(t,t,xt,0,0) WITH grad, form x1_hat = xt+(1-t)*v, compute loss = ||F(x1_hat)-d||², backprop to xt via autograd. Apply as guided_velocity = v.detach() + grad_zt (the FMRG-J update). Add `conf/method/fmrg_j.yaml`. Start with (A) first since it reuses existing code; (B) after.
- 0004_inversion/fmrg_e first run — guidance sweep {0.3,1.0,3.0} + normalize_grad={T,F} on single map 6044, esd_teacher ckpt, 200 steps, n_samples=4 (800 solves). BEST: normalize=T, g=0.3: MAE 0.137/SSIM 0.578/misfit_ratio 0.013. REFERENCE: flow_tilt g=1 norm=T: MAE 0.127/SSIM 0.635/misfit_ratio 0.072. FMRG-E does NOT beat flow_tilt at matched solve budget (n_opt=1). SHARP CLIFF: g=0.3 → good (MAE 0.137), g=1.0 → broken (MAE 0.676), g=3.0 → worst (MAE 0.899). normalize_grad=False at g=1.0 diverges completely (MAE 0.884, misfit_ratio 2.07 — guidance INCREASES the data error, raw seismic gradient is enormous). KEY MECHANISM: FMRG-E at g=0.3 drives misfit_ratio to 0.013 (5× lower than flow_tilt's 0.072) yet has WORSE model error (MAE 0.137 vs 0.127) — fits data harder but goes more off-manifold. Two reasons: (1) wt=(1-t)*t_next decays to zero at t≈0, suppressing guidance during the early trajectory steps where rough velocity structure forms (flow_tilt applies constant guidance from step 1); (2) inner gradient is in x1-space and NOT manifold-aligned — FMRG-E's n_opt=1 single step in x1-space differs from flow_tilt's chain-rule gradient in xt-space which implicitly projects onto the trajectory tangent. BOTTOM LINE: FMRG-E (n_opt=1) is a DPS variant with FMRG time-weighting, but the wt schedule that's optimal by control theory is suboptimal for the FWI recovery objective under the inverse crime. (runs/0004_inversion/flow_map_fmrg_e_2026-07-03T00-08-01Z/{0,1,2}; 5n5nfb0z)
- 0004_inversion/ot-misfit (plumbing, not yet run) — the guidance data-misfit is now a knob: `method.misfit=l2|ot` (+`method.ot_k`), where `ot` is the Peng et al. 2026 OT potential (frozen amplitude weighting → per-trace shift/normalize → quantile-form W2 → observation-scale normalization; physics.misfit.OTMisfit), threaded as an optional `misfit_fn` through guided_sample / fmrg_e_sample / dps_sample / red_diffeq_sample / make_misfit_reward — misfit=l2 keeps every existing path byte-identical, and eval metrics (MAE/RMSE/SSIM, L2 misfit ratio) never follow the guidance misfit. DELIBERATELY NOT Peng's full Algorithm 2: their preconditioned guidance (rho_i*D_i) is left out so L2-vs-OT isolates the potential (an ablation their paper lacks). UNIT-TEST FINDING (tests/test_misfit.py, 9 pass, CPU): the W2 numerics are exact (translated positive pulses → perfectly linear misfit-vs-shift), BUT for an isolated zero-mean Ricker the shift-to-positive normalization saturates almost as fast as L2 (positive-lobe excess absorbed by the adjacent negative lobe — transport stays local; Métivier's critique); the long-range shift discrimination in Peng's construction comes from the observation-frozen amplitude weighting (k=100: ~28% spread across shifts where L2 is exactly flat), and is NOT monotone. Expect the OT edge to be modest under the noiseless inverse crime; NOT YET RUN on RunPod (smoke + L2-vs-OT sweep pending). For mfm_g note sigma: J is O(1) vs L2's O(1e1) sums, so sigma≈100 (tuned for L2) needs retuning under ot. [RUN 2026-07-05 — see the L2-vs-OT entry below]
- 0004_inversion/ot-misfit VERIFIED on RunPod (2026-07-05) — full suite 88 passed/4 skipped (incl. test_tilt/test_flowmap_steer/test_diffusion_dps/test_red_diffeq/test_inversion_eval); flow_tilt smoke (esd_teacher ckpt, steps=20, n=2, map 6044) passes all gates: both misfits complete, ot run named `invert-…-ot`, guidance descends its own J (0.0253→0.0061, O(0.01) as designed), and misfit=l2 is UNCHANGED vs pre-change commit 90ec449 at the same seed — full-precision metric deltas ≤7e-7, the same magnitude as re-running identical code twice on this GPU (float nondeterminism), so "byte-identical" holds to hardware limits. FIX REQUIRED (commit 1055871): 035dbe7 declared misfit/ot_k only in MethodConfig, not in any conf/method/*.yaml, so Hydra struct mode rejected `method.misfit=ot` on every method; keys added to the six guidance yamls (defaults unchanged).
- 0004_inversion/L2-vs-OT first real comparison (single map 6044, esd_teacher ckpt, 200 steps, n_samples=4, matched solves per method; wandb online) — the OT potential BEATS L2 on model error for BOTH methods, stronger than the "modest edge" the unit tests predicted. flow_tilt (800 solves, g=1 norm=T transferred with NO retune, as expected): ot MAE 0.109/RMSE 0.146/SSIM 0.682 vs l2 0.126/0.168/0.637 — ~14% MAE cut and the best flow-map-prior result on this map to date, at slightly WORSE final L2 data fit (misfit_ratio 0.059 vs 0.049): the OT gradient direction buys model accuracy it does not buy (and does not need) in L2 data fit. mfm_g (3200 solves, σ resweep {0.03,0.1,0.3} as planned): monotone in range, σ0.03 best — MAE 0.128/SSIM 0.518/misfit_ratio 0.115 vs the L2-tuned best (σ100) 0.195/0.431/0.077; that nearly closes the gap to flow_tilt·l2 on MAE (0.128 vs 0.126) though SSIM still trails (0.518 vs 0.637), and σ0.03 sits at the SWEPT EDGE → extend to {0.003,0.01} next. σ0.1/σ0.3 under-steer (misfit_ratio 0.49/0.76). TRAJECTORY STABILITY (steps.jsonl data_fidelity = raw L2 MSE, comparable across misfits): OT is smoother — flow_tilt max single-step jump 0.176 (ot) vs 0.216 (l2); mfm_g·l2·σ100 has the largest spike of all six runs (1.13) vs ≤0.30 for every ot run. INTERPRETATION: under the noiseless inverse crime both misfits could reach ~zero, so the edge is not anti-cycle-skipping (no wavelet/operator mismatch here) — the observation-frozen amplitude weighting acts as a better-conditioned preconditioner on the residual, improving the DIRECTION of the guidance gradient (same lever the fmrg_e/mfm_g entries identified as the bottleneck). CAVEATS: n=1 map, single seed, inverse crime; the mfm_g gain conflates misfit geometry with temperature retuning (though both misfits got their own σ sweep, granularity ~3×); σ sweep boundary not closed. NEXT: extend mfm_g σ sweep below 0.03; multi-map eval.py sweep l2-vs-ot for flow_tilt; the noise_std matched-σ experiment is where OT should shine properly. (runs/0004_inversion/flow_map_flow_tilt_2026-07-05T{10-29-51,10-35-14}Z, flow_map_mfm_g_2026-07-05T{10-40-11,10-46-11,10-52-38,10-59-08}Z; note flow_tilt·l2 ran at commit 1055871, the rest at c9e98e3 — that commit only adds steps.jsonl logging, and mfm_g·l2·σ100 reproduces the 2026-07-02 journal numbers exactly (0.195/0.431/0.077), confirming unchanged numerics; flow_map_mfm_g_…10-40-11Z/config.json has a torn/duplicated JSON tail — harmless, summary.json is clean, cause not chased)
- 0004_inversion/L2-vs-OT across ALL prior families (map 6044, 200 steps, n=4, 800 solves each, defaults per method yaml; wandb qdhdzmo3/0sqtvodl/g3mxl5p5/z7a57sxz + the flow_map pair below) — the OT edge GENERALIZES: every prior family improves on model error at matched budget with zero guidance retuning (normalize_grad absorbs the scale). flow_matching·flow_tilt: l2 MAE 0.128/SSIM 0.634 -> ot 0.107/0.714 (the new overall best on this map, beating the previous fm champion config). diffusion·dps (fresh unet prior openfwi_2026-07-05T11-52-01Z, g=0.5/norm=T): l2 0.172/0.491 -> ot 0.124/0.629 — the LARGEST relative gain; DPS, previously the weakest guided method, lands near flow_tilt territory under OT. flow_map·flow_tilt: 0.126/0.637 -> 0.109/0.682 (2026-07-05 morning entry). flow_map·mfm_g σ0.03: 0.195/0.431 -> 0.128/0.518. PATTERN: in all four pairs OT fits the *L2 eval misfit* less or comparably tightly yet recovers a better model — consistent with the amplitude-weighted potential acting as a residual preconditioner that redirects (not strengthens) the guidance gradient; the weaker/more fragile the method's own gradient direction (dps, mfm_g), the bigger the OT gain. CAVEAT: single map/seed, inverse crime. Also fixed fmrg_e.yaml defaults (g=1.0/norm=F was the sweep-proven divergent config -> 0.3/T) and flow-map x_t viz (model-res 64 state now bilinear-resized to native 70 for the trajectory grid instead of silently skipped).
┌───────────────────────────┬─────────┬───────────────┬───────────────┬─────────────────┬────────────────┐
│      prior · method       │ misfit  │      MAE      │     SSIM      │ L2 misfit ratio │     wandb      │
├───────────────────────────┼─────────┼───────────────┼───────────────┼─────────────────┼────────────────┤
│ flow_matching · flow_tilt │ l2      │ 0.128         │ 0.634         │ 0.097           │ qdhdzmo3       │
├───────────────────────────┼─────────┼───────────────┼───────────────┼─────────────────┼────────────────┤
│ flow_matching · flow_tilt │ ot      │ 0.107         │ 0.714         │ 0.040           │ 0sqtvodl       │
├───────────────────────────┼─────────┼───────────────┼───────────────┼─────────────────┼────────────────┤
│ flow_map · flow_tilt      │ l2 / ot │ 0.126 / 0.109 │ 0.637 / 0.682 │                 │ (this morning) │
├───────────────────────────┼─────────┼───────────────┼───────────────┼─────────────────┼────────────────┤
│ flow_map · mfm_g σ0.03    │ l2 / ot │ 0.195 / 0.128 │ 0.431 / 0.518 │                 │ sv73iql9       │
├───────────────────────────┼─────────┼───────────────┼───────────────┼─────────────────┼────────────────┤
│ diffusion · dps           │ l2      │ 0.172         │ 0.491         │ 0.020           │ g3mxl5p5       │
├───────────────────────────┼─────────┼───────────────┼───────────────┼─────────────────┼────────────────┤
│ diffusion · dps           │ ot      │ 0.124         │ 0.629         │ 0.006           │ z7a57sxz       │
└───────────────────────────┴─────────┴───────────────┴───────────────┴─────────────────┴────────────────┘

OT improves model error for every prior family with zero guidance retuning, and flow_matching+ot (MAE 0.107, SSIM 0.714) is the new best result on this map. Most striking is DPS: historically the weakest guided method, it jumps from 0.172 to 0.124 — the pattern across all pairs is that the weaker the method's own gradient direction, the more the OT potential's amplitude-weighted preconditioning helps. All in the journal (f2caf4a), all runs with paired trajectory grids on wandb.
- 0002_flow_map/bf16-precision DECISION (2026-07-07) — bf16 (autocast loss forward; fp32 weights/optimizer) is the standing precision for openfwi runs: matched 1-epoch teacher-mode probes at b128 (RTX PRO 4500 Blackwell, 32 GB) measured 3.34 vs 2.50 it/s (+34%), 67 vs 88 s/epoch, peak 24.4 vs 28.4 GB, per-step losses within ±1% across the whole epoch and val/loss 360 (bf16) vs 370 (fp32) — parity within noise. b256 OOMs even under bf16 (peak 31.8 GB > 31.4 available), so batch stays 128 and bf16's win is speed, not batch size. Probe logs: runs/0002_flow_map/_probes/0002_b128_teacher_{fp32,bf16}_probe.log + teacher_probes_status.log; wandb runs openfwi_mf_2026-07-07T{10-25-58,10-33-05}Z. Decision noted at the `precision` default in both training yamls. Context: the same session diagnosed the overnight SIGKILLs (epoch-80, epoch-18 kills = launching session teardown, pod never restarted → all long runs now `setsid`), landed checkpoint resume support (3b6cfd1: train_state in checkpoints + training.resume_from), and fixed the Hydra struct trap that had made `training.precision=bf16` unrunnable from the CLI (precision/resume_from/num_workers were missing from conf/training yamls).
- 0001_flow_matching/openfwi_full DEFINITIVE (2026-07-08) — the full-OpenFWI flow-matching prior (plan Task 5) is trained and passes review: dit_b (134M), all 10 families (423k train / 47k val, per-family split, hflip), bf16, 90 epochs = 297,360 steps @ b128 / lr 1.4e-4 (32 GB override), 20.1 h on RTX 5090 (548 maps/s, 24.8 GB). Final val/loss 208.5; per-family val losses ordered by structural difficulty (FlatVel_A 89.8 → CurveFault_B 321.1), every family improving to the end. Per-family energy distance (512-sample pool vs family val) matches the perfect-model floor — a real-only control (proportional real mixture vs each family, seed-disjoint) reproduces every number within ±0.28: CurveVel_B/FlatVel_B ≈ 14.3 vs floors ≈ 14.5 (those families are intrinsically far from the 10-family mixture marginal), all others 1.5–3.7 vs matching floors. No starved family, no mode collapse; epoch-89 grid shows crisp flat/curved layers, fault offsets, Style textures. Checkpoints: volume keeps best (step_84) + final (step_89) raw+EMA with train_state; wandb keeps final EMA (openfwi-model-ema:v34) + weights-only final raw (openfwi-model:v35), 1.07 GB total after deleting the auto-uploaded best pair and the 2.14 GB train_state-laden raw (free-plan quota 5 GB was already blown at 8.2 GB account-wide). Harness now uploads finals-only and strips train_state from raw uploads (spec §6.1). Teacher for 0002: runs/0001_flow_matching/openfwi_2026-07-07T11-19-11Z/checkpoints/step_89_ema.pt. 0002 dit_b teacher-regime VRAM probes on the 5090: b128 bf16 OOMs at step 0 (needs +192 MiB over 31.4 GB) — 32 GB cards cannot run the definitive 0002 at b128; b64 probe in _probes/0002_full_teacher_b64_bf16_probe.log.
- 0003_diffusion/openfwi_full DEFINITIVE (2026-07-08) — the full-OpenFWI diffusion prior (plan Task 6) is trained and passes review: dit_b (134M), all 10 families (423k train / 47k val, per-family split, hflip), bf16, 60 epochs = 198,240 steps @ b128 / lr 1.4e-4, 13.7 h on the 5090 pod. Run runs/0003_diffusion/openfwi_2026-07-07T23-26-16Z, wandb 984t0883. Final val/loss 5.97e-3 (ε-MSE); per-family val losses ordered by structural difficulty (FlatVel_A 1.22e-3 → Style_B 1.33e-2), every family improving to the end. Per-family energy distance (512-sample pool vs family val) vs a seed-disjoint real-mixture floor control (proportional seed-1 mixture, same evaluator): every family sits at the perfect-model floor within the +/-0.28 control noise (max abs(gen-floor) = 0.31), so no meaningful per-family under-coverage. The fault families are dead-on floor (CurveFault_A -0.05, CurveFault_B +0.08, FlatFault_A -0.14, FlatFault_B -0.02); the only small positive deltas fall on CurveVel_A/FlatVel_A/Style_A (+0.24/+0.27/+0.31), all within noise (an earlier draft mis-attributed a +0.25..+0.49 under-fit to the fault families, a seed-dependent within-noise fluctuation that does NOT reproduce; see floor_control_verification.txt in the run dir). CurveVel_B/FlatVel_B read 13.29/13.35 vs floors 13.57/13.62 (gen -0.28/-0.27 BELOW floor); the ~13.6 floors are intrinsic to the mixture-vs-family comparison (those two families are far from the 10-family marginal), not model defects. Epoch-59 grid shows every family archetype (flat/curved layers, fault offsets, both Style textures), crisp, no collapse. wandb slimmed to finals-only per spec §6.1: deleted the old-harness best pair (v27, 2.68 GB) and the train_state-laden final raw (v28, 2.14 GB); kept final EMA (openfwi-diffusion-ema:v28) + uploaded weights-only final raw (openfwi-diffusion:v29) = 1.07 GB. Volume keeps best (step_49) + final (step_59) raw+EMA with train_state. The 0003 pod has nothing left to do.
┌──────────────┬────────┬──────────┬───────────┐
│    family    │ gen ED │ floor ED │ gen−floor │
├──────────────┼────────┼──────────┼───────────┤
│ CurveFault_A │ 1.687  │ 1.734    │ −0.047    │
├──────────────┼────────┼──────────┼───────────┤
│ CurveFault_B │ 2.189  │ 2.114    │ +0.075    │
├──────────────┼────────┼──────────┼───────────┤
│ CurveVel_A   │ 1.570  │ 1.329    │ +0.241    │
├──────────────┼────────┼──────────┼───────────┤
│ CurveVel_B   │ 13.290 │ 13.570   │ −0.280    │
├──────────────┼────────┼──────────┼───────────┤
│ FlatFault_A  │ 2.094  │ 2.235    │ −0.141    │
├──────────────┼────────┼──────────┼───────────┤
│ FlatFault_B  │ 2.721  │ 2.741    │ −0.020    │
├──────────────┼────────┼──────────┼───────────┤
│ FlatVel_A    │ 2.167  │ 1.898    │ +0.269    │
├──────────────┼────────┼──────────┼───────────┤
│ FlatVel_B    │ 13.349 │ 13.621   │ −0.272    │
├──────────────┼────────┼──────────┼───────────┤
│ Style_A      │ 3.643  │ 3.330    │ +0.313    │
├──────────────┼────────┼──────────┼───────────┤
│ Style_B      │ 3.843  │ 3.870    │ −0.027    │
└──────────────┴────────┴──────────┴───────────┘

The ~13.3 outliers for CurveVel_B/FlatVel_B are floor artifacts, not model defects — the perfect real-mixture model also scores ~13.6 there (those families are intrinsically far from the 10-family mixture marginal), and the generated pool actually sits below floor. Every family is at floor within the ±0.28 control noise (max |Δ|=0.31). No family is meaningfully under-covered.

- 0004_inversion FULL-PRIOR SWEEP (2026-07-09) — first inversions on the definitive full-OpenFWI dit_b priors (0001 step_89_ema flow-matching, 0003 step_59_ema diffusion): 9 configs × 10 core median-TV benchmark targets (one per family, `*_08`), 200 steps × 4 samples = 800 solves guided, method-yaml defaults (flow_tilt g1/norm=T, dps g0.5/norm=T, red_diffeq η 0.1/0.05, classical·tv smooth/0.001), 70 runs, zero failures, run beside the live 0002 teacher training. MEANS (MAE/SSIM): fm·tilt·ot **0.252/0.405** (best) < fm·tilt·l2 0.284/0.338 ≈ dps·l2 0.286/0.349 < dps·ot 0.296/0.384 < red_diffeq·l2 0.299/0.315 ≈ red_diffeq·ot 0.301/0.322 < classical·tv 0.306/0.276 < fm·unguided 0.354/0.246 < diff·unguided 0.487/0.177. FINDINGS: (1) **The clean Tweedie mechanism comparison is a TIE under L2** — with matched dit_b priors (same backbone/data/floor-level coverage), flow_tilt 0.2844 vs DPS 0.2864; June's 2× flow-over-DPS gap (0.18 vs 0.33) was prior quality, not flow-vs-diffusion mechanism. Under OT the flow pulls clearly ahead (0.252 vs 0.296). (2) **OT-vs-L2 is method-dependent at multi-family scale**: for flow_tilt OT wins 10/10 targets on BOTH MAE and SSIM (mean −11% MAE, +0.07 SSIM) — the single-map finding fully generalizes; for DPS OT wins 6/10 and the mean flips only on one blowup (style_b 0.181→0.448, guided misfit ratio 2.5× worse — an instability, not a trend; excluding it OT wins 0.298→0.279); for RED-DiffEq OT is neutral (0.299 vs 0.301) with *worse* data fit (misfit ratio 0.11→0.15; η_data is tuned for the L2 gradient scale, and normalize_grad can't absorb OT's different curvature in an optimization loop). OT's SSIM gain holds for both Tweedie methods (+0.07 fm, +0.035 dps). (3) **Unguided controls differ hugely** (fm 0.354 vs ddpm 0.487 — the 200-step subsampled reverse chain of a 1000-step DDPM degrades unguided samples, consistent with the quality_vs_nfe DDPM curve) yet guided DPS fully recovers the gap: guidance cuts diffusion MAE 41% vs fm's 20%. (4) **Learned priors now beat classical·tv** (0.306, SSIM 0.276) on the multi-family median-complexity set — June's classical-ties-fm result was FlatVel_A-specific; classical still wins pointwise on style_a (0.169, best on that target) and curvefault_a. (5) CurveVel_B is the hardest target for every method (MAE 0.36–0.59); red_diffeq fails to fit its data entirely (misfit ratio 0.43–0.71). CAVEATS: single seed, n=1 target/family (family-level numbers are anecdotes; the means over 10 are the signal), inverse crime, dps/red_diffeq knobs not retuned for the new priors. Full table: runs/0004_inversion/_sweeps/results_full_priors_2026-07-08.md (aggregator + driver in the same dir); sweeps runs/0004_inversion/{flow_matching_flow_tilt_2026-07-08T17-48-17Z, diffusion_dps_2026-07-08T19-38-58Z, diffusion_red_diffeq_2026-07-08T21-27-48Z, flow_matching_unguided_2026-07-08T23-52-54Z, diffusion_unguided_2026-07-09T00-09-52Z, none_classical_fwi_2026-07-09T00-21-37Z}; wandb invert-* runs with paired figures. NEXT: the `*_09` twin targets double n cheaply; dps·ot style_b blowup worth a look; 0002 flow-map prior joins this table when its training lands.
- quality_vs_nfe ANALYSIS (2026-07-08) — quality-vs-NFE curves for the three FlatVel_A priors (experiments/0005_analysis/prior_quality_vs_nfe.py; 512-sample pools vs the seed-0 held-out split, 3 noise seeds, energy distance, real-vs-real floor 0.088): the 0002 flow map's off-diagonal delivers its value exactly where designed — 1-step samples score 0.31 vs FM-ODE 5.20 and DDPM 25.6 at 2 NFE, and the few-step sampler beats the same checkpoint's ODE sampler everywhere below ~16 NFE — but the headline crossover is modest: flow map @ 4 NFE (0.66) ≈ DDPM @ ~8 NFE, because FlatVel_A is easy enough that DDPM is near floor by 32 steps (0.10) and the teacher-class 0001 prior reaches the floor at 128–200 NFE (0.090) while the 20-epoch flow map plateaus at ~0.42–0.73 at every step count (its ceiling is checkpoint quality, not sampler steps — 1 NFE oddly scores best, and its ODE curve *worsens* past 16 steps, both consistent with distillation noise dominating). Caveat baked into the script: diffusers DDPMScheduler "leading" spacing truncates the reverse chain for non-divisor step counts (256→t≤765 scored 1.00, 512→t≤511 scored 22.5!) — DDPM NFE grids must divide num_train_timesteps (250/500 used). Outputs: runs/0005_analysis/prior_quality_vs_nfe_2026-07-08T16-26-21Z/{quality_vs_nfe.png,results.md}, wandb quality_vs_nfe/p3b2c5ri (first run eocy3ml7 superseded — it carried the truncation-artifact points; its local dir was deleted).
