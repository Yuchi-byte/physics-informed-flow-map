# Journal

One line per concluded finding. Newest last. Each line cites the framework
and run, and the headline metric.

Format: `NNNN_slug/variant — headline metric (evidence)`

<!-- e.g. 0001_mnist_pipeline/default — final loss 0.21 (200 steps, runs/0001_mnist_pipeline/2026-06-24T...Z) -->

- 0001_flow_matching/gaussians — pass: energy distance 0.009576 < 0.5 gate; 8 modes recovered (runs/0001_flow_matching/2026-06-24T204300Z)
- 0001_flow_matching/mnist — pass: final FM loss 158.02 < 240 gate; samples digit-like (runs/0001_flow_matching/2026-06-24T205632Z)
- 0001_flow_matching/gaussians — pass: energy distance 0.0104 < 0.5 gate (~2× the 0.0050 noise floor); epoch loop, 100 epochs / 200 sampler steps (runs/0001_flow_matching/2026-06-25T00-45-58Z)
- 0001_flow_matching/mnist — pass: final FM loss ~107 < 240 gate; clean digits across all 10 classes; DiT 256/6/8, 100 epochs, 200 sampler steps (runs/0001_flow_matching/2026-06-25T00-53-06Z)
- 0001_flow_matching/openfwi — energy distance 0.127 vs held-out reals (~1.4× the 0.090 floor); clean FlatVel_A flat-layer maps; DiT 256/6/8, 100 epochs, 200 sampler steps, EMA; best val_loss @ epoch 39; 500 steps worse (0.197) (runs/0001_flow_matching/2026-06-25T14-18-57Z)
- 0002_fwi_tilting/poc — DPS-for-flow inference-time tilting recovers a held-out FlatVel_A map: data misfit driven to 0.5% of unguided, best-of-4 posterior MAE ~300 m/s; recovered map on-manifold (top/bottom layers right, middle layer is the residual error). Finding: gradient normalization essential (raw guidance ~10% effect at best) (experiments/0002_fwi_tilting/poc.py; ζ=1.0, 200 steps) [CORRECTED 2026-06-25 — see below]
- 0002_fwi_tilting/poc — CORRECTION (2026-06-25): the above recovery claim does NOT yet stand. The target map was actually in the TRAINING set (global index 29999 lands in train under the seed-0 split), not held out; the headline "best-of-4 MAE" was an oracle best-of-N selected with ground truth (unavailable in a real inversion); and d_obs was generated by the same noiseless forward operator used in the guidance term (an inverse crime). poc.py is now fixed to (a) draw the target from the genuine validation split, (b) seed the run, (c) require an explicit --ckpt, (d) clamp velocities to the trained range, and (e) also report mean/median/worst MAE plus a ground-truth-free (min-data-misfit) pick. The recovery claim must be RE-RUN on a held-out map before it holds; no replacement numbers are asserted here.
- 0003_baselines/train_diffusion — diffusion-prior + canonical DPS baseline (camp-A comparison) added: diffusers UNet2DModel prior + DDPM training (physics_informed_flow_map.baselines.{build_denoiser,train_diffusion_prior}) and DPS sampler that backprops the wave-equation likelihood through the denoiser (baselines.dps_sample), inverting the same held-out map through the same Deepwave operator as the flow PoC. Plumbing verified (--smoke: misfit ratio 0.044). NOTE: same held-out-map caveats as the poc correction apply (re-run on a genuinely held-out, seeded, ground-truth-free basis before claiming recovery). Full-quality run + quantitative head-to-head (MAE/SSIM/#solves) deferred to the comparison spec. [REFACTORED 2026-06-26: train_diffusion.py → inversion.py — a Hydra entry that LOADS a run.py prior checkpoint (no inline retraining), draws the target from the genuine seed-0 validation split (leakage fixed, matching the poc correction), and selects the baseline via a `method` config group (dps | unguided; --multirun sweepable). run.py trains the prior; inversion.py inverts.]
- inversion comparison (flow tilting vs diffusion DPS) — first multi-map head-to-head via the shared `physics_informed_flow_map.inversion` Evaluator over 12 held-out FlatVel_A val maps, matched cost (800 PDE solves/inversion), guidance sweep {0.3,1.0,3.0}. Flow tilting (0002, DiT flow prior step_39_ema) beats diffusion DPS (0003, UNet prior step_99_ema) at EVERY config: GT-free MAE ~300-360 vs ≥426 m/s, oracle MAE ~207-227 vs ~357-667, SSIM ~0.78 vs ~0.72-0.77. Flow is robust across guidance; diffusion DPS is fragile (best @ g=0.3, degrades to 753 @ g=3). KEY: the GT-free (min-misfit) pick trails the oracle by ~100-150 m/s for both — posterior selection, not prior quality, is the bottleneck; posterior-mean is no better. CAVEAT: NOT a clean mechanism comparison — priors differ (well-tuned flow vs fresh diffusion), so the gap conflates prior quality with tilting-vs-DPS; single seed, n=4, inverse crime. Reproduce: notebooks/inversion_eval.ipynb (scratchpad multimap_sweep.py).
- inversion comparison (all camp-A inverters, normalized-scale) — 9 methods over 4 held-out FlatVel_A maps, matched 800 PDE-solves/inversion, metrics = expected MAE/RMSE/SSIM across posterior samples on the [-1,1] OpenFWI scale (notebooks/run_inversion_eval.py; runs/inversion_eval/results.md). Ranking by MAE: fm·tilt·g1 0.176 / SSIM 0.503 (best model error + structure); red_diffeq·dit 0.180; classical·tv 0.181 (lowest data misfit 1.8e3 — fits data best with NO learned prior); flowmap·tilt·g1 0.189; unguided controls fm·g0 0.292 / flowmap·g0 0.305; classical·tikhonov 0.315 (TV >> Tikhonov for blocky layered media); diff_dit·dps·g0.3 0.325; diff_unet·dps·g0.3 0.341. FINDINGS: (1) wave-equation guidance cuts MAE ~40% vs unguided prior; (2) flow-matching tilt is the best method on model error + SSIM; (3) the optimization baselines (RED-DiffEq, classical-TV) tie fm·tilt on MAE and crush it on DATA misfit — the inverse-crime/non-uniqueness gap (fitting noiseless data ≠ low model error); (4) DPS underperforms even the unguided flow prior, but its g=0.3 is UNtuned. CAVEATS: inverse crime (noiseless, same operator) favors the data-fitting optimizers; n=4, single seed; DPS guidance not swept.
- 0002_flow_map/teacher-distill — esd_teacher distillation of the flow-map prior from the 0001 flow-matching prior (warm-start + off-diagonal esd_teacher target; 100 epochs FlatVel_A; runs/0002_flow_map/2026-06-27T01-44-09Z). vs the from-scratch (mf) flow-map on 4 held-out maps @ 800 solves (tilt g1): SSIM 0.444 -> 0.496 (to flow-matching parity 0.502), MAE 0.189 -> 0.192 (a wash, within the ±0.05 across-map std), RMSE 0.246 -> 0.261. FINDING: distillation buys flow-matching-like STRUCTURE (SSIM) but not its lower model error (MAE) — the flow-map still trails the fm champion (MAE 0.176) on model error. Honest, modest: SSIM is the clear signal; MAE differences across the three priors (0.176/0.189/0.192) are within ~1 std at n=4. CAVEAT: n=4, single seed, inverse crime.
